# commit-msg 红线门禁 · 本地 patterns 模板
# 用法：cp 本文件为同目录 commit-msg-patterns（gitignore 已排除），逐行填入黑名单称呼
# 规则：每行一条固定字符串（hook 用 grep -F 精确匹配）；文件本身绝不入公开仓——
#       黑名单清单只走 mentor 内部口径，公开仓规则正文只写中性描述（脱敏铁律）
# 示例行（占位，勿直接使用）：
# 学员真名
# 家庭成员真名
# 本机用户名
