# W02-L02 · 字符串功夫 — homework 评语

> **学员:** Tree（猴哥）· **mentor:** 小马（MK-002 野・行动，代如意执行）
> **日期:** 2026-09-13
> **文件:** `~/learning-homework/tree/homework/W02-L02-homework.py`（534B）
> **验证:** mentor 实跑通过（输入 Monkey / "   Graystripe   " / Firestar），编译零错误

---

## 整体评分：⭐⭐⭐⭐ 良好（86/100）

5 题全交，🚀 加速挑战（Tree 建议做）主动做满。题 1-3 全对，题 4 会做但换了题面，题 5 暗号对、侦探翻车——详见成长豆 ①，那是这批作业里最值得修的一行。

## 亮点（3）

1. **题 2 五连全对**：F / t / Fire / heart / ir ——「含头不含尾」口诀已内化，`s[1:3]` 这种最容易翻车的正向切片也对。practice 里你两种写法（`s[:4]` / `s[0:4]`）都敲过，作业里稳稳落地。
2. **题 3 实测干净**：实跑输入 `"   Graystripe   "`，strip 前后 16 → 10，数字精确——不是"看起来对"，是**跑出来对**。
3. **题 5 暗号部分完全正确**：`fav_cat[0] + "..." + fav_cat[-1]` 首尾索引 + 拼接一气呵成，Firestar → `F...r`。挑战题的"暗号"半场满分。

## 成长豆（3）

1. **题 5 侦探翻车（双重 bug，本份作业唯一必改项）**：
   ```python
   print("start" in hidden)   # 你写的
   print("star" in fav_cat)   # 讲义答案
   ```
   两处偏差叠加：**词拼错了**（start ≠ star）+ **查错了对象**（在暗号 `hidden` 里找，不是在原名 `fav_cat` 里找）。暗号只剩首尾两颗珠子（`F...r`），任何完整的词都不可能藏在里面——所以这行**永远输出 False**，无论输入什么。in 侦探的要点就是：**去原串里查，别去加工过的串里查**。修这一行即可。
2. **题 4 换了题面**：题目指定 `"Firestar"`（答案 `Fir*star`），你用的是 `"Fireheart"`（得 `Fir*h*art`）。replace 用法本身完全正确，但作业按题目对答案——判分只能给方法分，不能给全分。下次看清题面里的"原料"再开工。
3. **题 3 提示语两处小疵**：`"Enter you name"` → `"Enter your name"`（拼写）；`"trailing spaces"` 只指尾空格，题目是**前后**都有——`"leading and trailing spaces"` 才对。提示语是写给用户的，措辞也算代码质量的一部分。
4. **（承接 W02-L01 成长豆 ①，第 2 次出现）** 题 2"先猜再跑"这次仍是直接跑。上次建议的"先 `#` 注释写猜测 → print 验证"两步走没落地。猜错才有肌肉记忆——这是第二次提，下次作业里请真的看到那行猜测注释 😼

## 通过结论

✅ **通过 — 良好。** 五招基本功（len/upper/lower/strip/replace）+ 切片全部过关。

**进 W02-L03「f-string：漂亮地说话」前的唯一动作：把题 5 那一行修掉，自己跑一遍看到 True。** 修完即可自主进入下一课，不用等我。

---

*归位备注（⑩）：本次检查顺带把 w02-l01-*.py / w02-l01-*.review.md 统一 rename 为 W02-L01-* 大写命名，与 ⑨ 约定对齐，仅文件名、未动内容。*
