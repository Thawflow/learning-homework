# Tree 作业评语 · 20260902-chatroom.py

> Mentor：小马 🐴  · 2026-09-02 19:36
> 文件路径：`~/learning-homework/tree/homework/20260902-chatroom.py`

## 归位结果（自动修正 · 规则 ⑩）

| 项 | 原状态 | 修正后 |
|----|--------|--------|
| 位置 | `tree/` 顶层 | `tree/homework/` ✅ |
| 命名 | `20260902-chatroom.py` | 保留原名 ⚠️ |

**命名存疑：** 不符合 `W0X-L0X-{homework,practice}.py` 约定。从内容看融合了 W01-L02（变量+注释+大坑验证 + 逗号分隔）和 W01-L03（input + int 转换）外加 W02 进阶 f-string（`{age + 1}`），无法可靠推断 lesson 编号 → **列晨会确认项**，请 Tree 拍板是否需要重命名为 `W02-补充-practice.py` 或类似。

## 跑验证（python3 + stdin）

输入：`小焰\n松鼠\n9` → 输出：

```
我是猴哥，30岁
我是猴哥， 30 岁
我是猴哥，30岁
明年我就31岁啦
你叫什么名字？最喜欢什么猎物？多大了？我是雷族的小焰，最爱松鼠，明年就10岁了
```

✅ 跑通零报错。无 input 之外任何异常。

## 亮点 ✨

1. **四种拼接横向对比齐全** — `+` 拼接（含 `str()` 转换）、`,` 分隔、`f-string` 基础、`f-string` 内嵌表达式（`{age + 1}`），难度梯度合理，是 W01-L02 练习 5 的自然延伸。
2. **注释意图明确** — 每行 print 后紧跟注释解释机制（无空格 / 前后各加空格 / 自动替换 / 表达式求值），符合 W01-L02 § 五「备注说『为什么』就够」的指引。
3. **主题贴合** — 用 Warriors「雷族」写情境题，呼应 W01-L02 的 ThawPaw 兴趣设定，让抽象语法有了情感锚点。
4. **小任务组合得当** — `input` 三连（名字/猎物/年龄）+ `int()` 转换 + f-string 综合题，一次练穿 W01-L03 的「致命细节」。

## 成长点 🌱

1. **题2 大坑注释已补 ✅** — `print("...", age, "岁")` 后的「数字会在前后各加一个空格」点出了 `sep=' '` 默认值的要害（核心解释到位），符合 W01-L02 待改项要求。**顺带验收：这条已通过。**
2. **可再加一句「为什么」** — 大坑注释目前只描述现象，可补一句机制：「因为 print 默认 `sep=' '`，逗号分隔的多个参数之间会自动加空格」。一句话就够，把「是什么」升级到「为什么」，未来举一反三更稳。
3. **input 的健壮性留白** — `int(input(...))` 若用户输字母会崩。这超出本次作业范围（W01-L03 暂未涉及异常处理），列为「未来坑」即可，不必现在就处理。
4. **「我是 30 岁」 vs 「我是30岁」空格差** — 第一行手动 `str()` 拼接（无空格），第二行逗号分隔（有空格）。这是个隐藏对比实验，建议在文件顶部加一行注释「# 演示：+ 号无空格 vs 逗号有空格」，让未来回看的自己一眼明白意图。

## 结论

- **本次提交：通过 ✅ · 5 / 5 分**
- **W01-L02 待改项验收：已通过**（题 2 大坑注释到位）
- **晨会待确认：** 命名 `20260902-chatroom.py` 是否需要重命名 + lesson 编号归属

继续往前冲 💪
—— 小马 🐴
