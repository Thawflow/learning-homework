# W02-L01 · 数字与类型 — homework 评语

> **学员:** Tree（猴哥）· **mentor:** 小马(MK-002 野・行动,代如意执行)
> **日期:** 2026-09-08
> **文件:** `~/learning-homework/tree/homework/w02-l01-homework.py`(639B)

---

## 整体评分:⭐⭐⭐⭐⭐ 优秀(95/100)

5 道题全完成,🚀 加速挑战(题 5:3661 秒换算)主动做完 — 课程明确写「ThawPaw 选做,Tree 建议做」,Tree 把它做满,值得加鸡腿 🍗。

## 亮点(3)

1. **#1 透视镜练习**:`"42"` 和 `"ThawPaw"` 的对照,把 W01 埋的雷(int 输入是 str)顺手再钉一颗钉,排雷意识到位。
2. **#4 猫粮小账本**:四个变量命名(`price`/`quantity`/`amount`/`total`/`change`)语义清晰、命名规范,比讲义示例还多一步算找零,实战感强。
3. **#5 🚀 3661 秒换算**:用 `// (60*60)` 和 `% 60` 一行拆完,没用临时变量(没建 `total = 3661`),直接拿字面量 — 简洁干净。运行结果 1小时1分1秒,完全正确。

## 成长豆(2)

1. **#2 三连的"先猜再跑":** 课程正文明确写"先猜再跑"是练习意图。作业里你直接 print 了答案 —— 跑前在草稿纸写一行猜测(应该写 `20262026` / `4052` / `101010`),会更贴近练习设计意图,猜错才有真正的肌肉记忆。下次可以走"先 # 注释猜测 → print 验证"两步走。
2. **`input()` 的 EOFError 演练价值:** 你在 #1 第 5 行写了 `print(type(input("随便输入：")))` —— 这条练习很妙,直接印证讲义"input 出来永远是 str"的铁证。但是这条要 interactive 跑才能看到结果(非交互 stdin 会 EOFError)。下次可以在题首加一句 `print("# 提示:运行后会等你输入")` 提醒自己,或者拆成两行:
   ```python
   x = input("随便输入：")
   print(type(x))
   ```
   —— 跑通率更高,跟 #3/#4 的模式统一。

## 通过结论

✅ **通过 — 优秀。** 三兄弟(int/float/str)用法扎实,转换修复路径清晰,3661 秒挑战主动做完。

进入 W02-L02「字符串功夫」。