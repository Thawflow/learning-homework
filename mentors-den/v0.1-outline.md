# mentors-den · v0.1 Outline（v1.7 命名规范化草案）

> 心 9/17 12:17 BJT 起草 · 待 main session 完整化 + 猴哥拍板 + 苗苗 review pass
> 现行 v1.8 = mentor rules v1.8（mentors-rules.md，commit 9462，2026-09-08 12:35 落档）
> v1.7 = 命名规范化下一步（不在 v1.8 内容上做实质变更，仅改名 + 路径）

## 1. 目标与不变项

**目标**：让 v1.6 文件名 → v1.7 命名，content 跟随现行 v1.8 rules，**不引入任何内容变更**。

**不变项**：
- 现行 v1.8 rules 内容（3 条底线 + 4 条如意补 + 1.2/1.4 节奏修订）= 唯一真理
- 单签 + chatroom review 工作流（v1.8 Tree 2026-09-08 12:43 授权）
- git 历史完整保留（rename 用 `git mv` 而非 rm + add）

## 2. 改名映射（决策点 A）

| 旧名 | 新名 | 内容映射 | 备注 |
|------|------|----------|------|
| `mentors-rules.md`（v1.6 风格遗留） | `mentors-rules-v1.7.md` | content → v1.8 rules（3+4+1.2+1.4） | 主文件 |
| `ima-final-v2.md` | `ima-final-v2.md` | 不变 | IMA 已是现行版本 |
| `review-template.md` | `review-template-v1.7.md` | content → v1.8 配套模板 | v1.7 配套 |
| `course/` | `course/` | 不变 | 课程目录独立 |

**选项**：
- A1. 全加 `-v1.7` 后缀（强一致）
- A2. 仅主文件加后缀，其他保持原名（最小改动）
- 🎯 **建议 A2**：仅 `mentors-rules-v1.7.md` + `review-template-v1.7.md` 加后缀，IMA 保持原名（IMA 不属于 v1.x 命名序列）。

## 3. 分仓方案（决策点 B）

**选项**：
- B1. **`mentors-den/` 仓内子目录**（同 repo `learning-homework`，sub-path）— 简单，git 历史不破
- B2. **`mentors-den/` 独立仓**（独立 repo，独立权限）— 严格隔离，但 git-filter-repo 风险
- 🎯 **建议 B1**：当前 `learning-homework/` 仓内已有 `mentors-den/`，B1 是零成本延续；B2 需要 git-filter-repo + force push（铁律#6 高危，需猴哥直回 iMessage 授权）。

## 4. commit 流程（既定）

```
git mv <old> <new>
git add mentors-den/v0.1-outline.md
git commit -m "chore(mentors-den): rename v1.6 → v1.7 (no content change)"
git push origin main  # 单签，chatroom review 已通过 v1.8 (12:43 BJT 9/8 授权)
```

- 单签（冰爪或如意均可）
- chatroom review 已替代 v1.3 双签（不再需要 master 拍板等待）
- 修订 → 新 commit，禁止 rm + add 重建

## 5. push 时机（既定）

- ✅ 立即可行 — v1.8 chatroom review 工作流 9/8 12:43 已授权
- ✅ 内容侧无变更 — diff 应为空（除文件改名）
- ⚠️ 验证：push 后跑一次 `diff mentors-rules-v1.7.md mentors-rules.md` 应为 0 行（除 header 标 v1.6 → v1.7）

## 6. 风险与回滚

| 风险 | 缓解 |
|------|------|
| 误改 content | push 前 diff 必须为空 |
| git 历史断 | 用 `git mv` 而非 rm + add |
| 路径冲突 | mentors-den/ 已是独立 sub-path，无冲突 |
| 学员引用旧名 | 更新 `course/W0X-L0X.md` 内引用（grep 验证） |

**回滚**：`git revert <commit>` + push（无 force-push 需求，保留完整历史）。

## 7. 待办 checklist

- [ ] 猴哥拍板分仓方案（B1 vs B2）
- [ ] 猴哥拍板改名映射（A1 vs A2）
- [ ] 苗苗 review pass（虽然不是内容变更，但她是使用者）
- [ ] push 前 grep 验证：`grep -rn "mentors-rules.md" course/` → 应 0 命中（除 `course/W0X-L0X.md` 已迁移引用）
- [ ] push 后跑 test.sh L1 全绿 + BUILD_TIME 验证

---

**起草**：冰爪 ❄️ 2026-09-17 12:17 BJT（todo-hourly 12 班 partial advance on L74）
**关联**：TODO L74 / L75（ABC 评级独立起草）
**下一步**：main session review + 猴哥拍板 B1/B2 + A1/A2 二选一