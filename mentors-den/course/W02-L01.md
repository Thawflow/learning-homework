# W02-L01 · 数字与类型（"2026" ≠ 2026）

> 用时：30-45 分钟 · 难度：⭐⭐ · 前置：W01 三课

> 📁 **课前准备（开课第一件事）：** 打开 Thonny，新建文件 `W02-L01-practice.py`（`文件 → 新建`），保存到 `~/learning-homework/<你的学员名>/homework/`（以 ThawPaw 为例：`thawpaw/homework/`，Tree 对应 `tree/homework/`；平铺不建周子目录）。本课所有亲手敲的代码都写进它——这是**随堂练习文件**，与课后作业 `W02-L01-homework.py` 是两个文件，导师靠它确认你真的跟着练了。

---

## 一、概念：数据也有"种族"（5 分钟）

W01 埋过一颗雷：input 收到的是话，要算数先 int。今天正式排雷——Python 眼里数据分**类型（type）**，类型不同，待遇天差地别。

本课三大主角：

| 类型 | 英文名 | 长相 | 例子 |
|---|---|---|---|
| 整数 | int | 没小数点、没引号 | `2026`、`11`、`-3` |
| 浮点数 | float | 带小数点 | `3.14`、`39.9`、`0.5` |
| 字符串 | str | 引号包住 | `"2026"`、`"喵"`、`"火心"` |

核心区别一句话：**"2026" 是写在纸上的字，2026 是能拿来算的数。**

猫武士式理解：喊"火心"这个名字，和火心真实扑向敌人的战斗力，是两码事。名字（字符串）不能加 1，战力（数字）可以比大小。

怎么选：数个数用 int，带小数的量用 float，一切话用 str。

## 二、type()：类型透视镜（5 分钟）

分不清时用 `type()` 照一照。敲进课前建好的 `W02-L01-practice.py`，亲手敲：

```python
print(type(2026))
print(type("2026"))
print(type(3.14))
print(type("3.14"))
```

输出（尖括号里的英文就是类型名）：

```text
<class 'int'>
<class 'str'>
<class 'float'>
<class 'str'>
```

第四行最阴险：**带引号的小数也是字符串**。

再给 W01 的大坑补个铁证：

```python
age = input("How old are you? ")
print(type(age))
```

不管输入 11 还是 abc，`type(age)` 都是 `str`——**input 收到的一切都是字符串**。

## 三、转换三兄弟：int() / float() / str()（10 分钟）

类型不是终身制，可以转换：

```python
print(int("2026") + 1)     # 2027: text "2026" becomes a number
print(float("3.5") * 2)    # 7.0: text "3.5" becomes a decimal
print(str(11) + " years old")   # number 11 becomes text, now it can join
```

### 逐字拆解 `int("2026")`

| 部件 | 作用 |
|---|---|
| `int` | 转换命令：目标是整数 |
| `(` `)` | 要转的东西放括号里 |
| `"2026"` | 原料：字符串版的 2026 |

结果得到**真正的整数** 2026，能参加一切数学运算。

最常用的组合拳（W01 见过第一个）：

```python
age = int(input("How old are you? "))
price = float(input("Price in yuan? "))
```

先由 input 收话，再转成数装盒：整数用 int，可能带小数用 float。

## 四、数字运算符全家福（8 分钟）

`+ - * /` 是老朋友，介绍三位新面孔：

| 运算 | 式子 | 结果 | 说明 |
|---|---|---|---|
| 真除法 | `7 / 2` | `3.5` | 结果**总是 float**，`8 / 2` 也得 `4.0` |
| 整除 | `7 // 2` | `3` | 扔掉小数部分 |
| 余数 | `7 % 2` | `1` | 7 里装满 3 个 2 之后剩下的 |
| 乘方 | `2 ** 10` | `1024` | 2 自乘 10 次 |

`//` 和 `%` 现在冷门，W03 判奇偶、W04 画图时天天用。

冷知识：`int(3.9)` 得 `3`——**直接砍尾，不四舍五入**。

## 五、⚠️ 转换翻车现场（8 分钟）

每个坑都值得亲手踩一次（都敲进 `W02-L01-practice.py`），读报错最后一行：

1. **int 不收带小数点的字符串**：`int("3.5")` → `ValueError`。字符串里的小数要找 float：`float("3.5")` ✅
2. **int 不收不是数的字**：`int("你好")` → `ValueError`（这串字变不成整数）
3. **字符串乘法是复读机**：`"10" * 3` 得 `101010`，不是 30！字符串遇到 `+` 是拼接、遇到 `*` 是重复，只有真数字才做数学
4. **该用 float 的地方用了 int**：问价格写成 `int(input(...))`，输入 12.5 → 当场 ValueError。规则一句话：答案可能带小数的问题，用 float 收

彩蛋（不是你的错）：Shell 里算 `0.1 + 0.2`，看看得多少——电脑的小数世界会多出一串零头。别慌，W02-L03 的 `:.2f` 一招治它。

## 六、动手练习（先做完，再看第七节答案）

> 📁 **作业保存规则：** 本节课后练习写进一个**新文件** `W02-L01-homework.py`（存 `~/learning-homework/<你的学员名>/homework/`，与随堂练习 `W02-L01-practice.py` 分开），每道题开头写一行备注分隔（如 `# --- 题 1 ---`）。**交完作业要跟你的 mentor 说一声（ThawPaw → IcePaw / Tree → 如意）才会批改**，评语出现在 `reviews/*.review.md`。

1. 透视镜练习：用 type() 查五样东西——`42`、`"42"`、`4.2`、`"ThawPaw"`、input 收到的回答。先笔猜，再运行
2. 先猜再跑三连：`"2026" + "2026"`、`2026 + 2026`、`"10" * 3`
3. 修 bug：`cats = input("How many cats at home? ")`，然后 `print(cats + 1)` 翻车了。抄下报错最后一行，再修复到能跑
4. 猫粮小账本：问单价（float）、买几袋（int）、付了多少钱（float），打印总价和找零
5. 🚀 加速挑战：输入总秒数（比如 3661），用 `//` 和 `%` 拆成 X 小时 Y 分 Z 秒（ThawPaw 选做，Tree 建议做）

## 七、练习答案（先做再看！）

**1.** 依次是 int、str、float、str、str。最后两个尤其记牢：input 出来的永远是 str。

**2.** `20262026`（拼接）、`4052`（数学）、`101010`（重复三遍）。

**3.** 报错最后一行：`TypeError: can only concatenate str (not "int") to str`。修复二选一：

```python
cats = int(input("How many cats at home? "))
print(cats + 1)
```

或 `print(int(cats) + 1)`。

**4.**
```python
price = float(input("Price per bag: "))
bags = int(input("How many bags? "))
pay = float(input("How much did you pay? "))
total = price * bags
print("Total:", total, "yuan")
print("Change:", pay - total, "yuan")
```
算得对就行，样例数据自己造。

**5.**
```python
total = int(input("Total seconds: "))
h = total // 3600
m = total % 3600 // 60
s = total % 60
print(h, "hours", m, "minutes", s, "seconds")
```
3661 秒 → 1 小时 1 分 1 秒。`total % 3600` 先甩掉整小时，剩下的再 `// 60` 拿分钟。

## 八、自查清单

- [ ] 随堂练习文件 `W02-L01-practice.py` 存了本课敲过的代码，能跑通
- [ ] 我能说出 int / float / str 三兄弟的长相区别
- [ ] 我会用 type() 透视数据，知道带引号的小数也是 str
- [ ] 我亲手踩过 int("3.5") 的 ValueError，读过报错最后一行
- [ ] 我能解释 "10" * 3 为什么是 101010
- [ ] 练习 5 题完成，答案对过；🚀 挑战题做过（ThawPaw 选做，Tree 建议做）

全勾 → 进入 W02-L02「字符串功夫」。
