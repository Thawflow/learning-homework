# W03-L03 · and / or / not：条件组队

> 用时：35-45 分钟 · 难度：⭐⭐⭐ · 前置：W03-L02

---

## 一、概念：复杂世界需要组合条件（5 分钟）

单一条件不够用了：

- 当上战士：要「满 12 个月**且**训练完成」——缺一不可
- 周末：是「周六**或**周日」——满足其一
- 不饿：not 饿

三个连接词：**and（且）、or（或）、not（非）**。Scratch 同学：就是运算积木里的「…与…」「…或…」「并非…」。

## 二、and：两个都要真（8 分钟）

```python
age = int(input("How many months old is this cat? "))
trained = input("Finished all training? (yes / no) ")

if age >= 12 and trained == "yes":
    print("Warrior name granted!")
```

输入 13 和 yes → 授予；13 和 no → 安静；5 和 yes → 安静。**and 左右都真，整体才真**，一假全假。

## 三、or：一个真就真（7 分钟）

```python
day = input("What day is it today? ")

if day == "Saturday" or day == "Sunday":
    print("Family coding time!")
```

周六 ✅，周日 ✅，周一 ❌。**or 左右至少一个真，整体就真**，全假才假。

## 四、not：反着说（5 分钟）

```python
hungry = True
print(not hungry)
```

输出 `False`。not 把真假对调。用得不多，但读别人代码常见，认得即可。

新手期大法：**给组合条件套小括号**，`(age >= 12) and (trained == "yes")`，谁先谁后一目了然，不怕记错优先级。

## 五、实战：成绩等级评定器（12 分钟）

需求：输入成绩（0-100）和出勤（0-20 次），规则：

- 成绩 >= 90 **且** 出勤 >= 16：A+
- 成绩 >= 80 **且** 出勤 >= 14：A
- 成绩 >= 60 **且** 出勤 >= 10：B
- 其余：C

新建 `grader.py`：

```python
print("=== Grade Judge ===")

score = int(input("Test score (0-100): "))
attend = int(input("Attendance (0-20): "))

if score >= 90 and attend >= 16:
    grade = "A+"
elif score >= 80 and attend >= 14:
    grade = "A"
elif score >= 60 and attend >= 10:
    grade = "B"
else:
    grade = "C"

print(f"Result: {grade}")
```

看两点：

1. elif 链从上往下、命中即止（上节课的灵魂）——进了 A+ 就不会再看 A
2. 每个条件都是 and 组出的两个维度：分数再高，翘课多了照样掉级

**规则是活的**：改成三科成绩？加一条努力分？随意。改完至少测四组数据：95/20、95/10、82/15、59/20，先笔算等级再运行。

## 六、⚠️ 新手三大坑（7 分钟）

1. **用 or 表达「中间范围」翻车**：`if score > 60 or score < 90:` 永远为真——任何数都至少满足一头。夹在中间要用 and：`score > 60 and score < 90`。
2. **字符串比大小**：`"9" > "10"` 居然是 True！字符串按字典序逐字比（'9' 排在 '1' 后面）。要比数值，先 int——W02-L01 的老朋友。
3. **括号里的等号**：套小括号是好习惯，但别把 `==` 手滑写成 `=`（W03-L01 的坑穿越而来）。

## 七、动手练习（先做完，再看第八节答案）

> 📁 **作业保存规则：** 本节课后练习写进一个**新文件** `W03-L03-homework.py`（存 `~/learning-homework/<你的学员名>/homework/`，与随堂练习 `W03-L03-practice.py` 分开），每道题开头写一行备注分隔（如 `# --- 题 1 ---`）。**交完作业要跟你的 mentor 说一声（ThawPaw → IcePaw / Tree → 如意）才会批改**，评语出现在 `reviews/*.review.md`。

1. 双人儿童票：问两个人年龄，都 <= 12 才打印 "You can buy the kids' duo ticket"
2. 周末判断器：输入今天星期几，周六或周日打印 "You can sleep in"
3. 先猜再跑：print(True and False)、print(True or False)、print(not True)
4. 电影半价票：年龄 <= 12 **或** >= 65 打印 "Half price: 20 yuan"，否则 "Full price: 40 yuan"
5. 🚀 加速挑战：奖学金候选人——三门课成绩（三个 input），全部 >= 60 **且**平均分 >= 85 打印 "Scholarship candidate"，否则 "Keep it up"（ThawPaw 选做，Tree 建议做）

## 八、练习答案（先做再看！）

**1.**
```python
a1 = int(input("First person's age: "))
a2 = int(input("Second person's age: "))

if a1 <= 12 and a2 <= 12:
    print("You can buy the kids' duo ticket")
```

**2.**
```python
day = input("What day is it? ")
if day == "Saturday" or day == "Sunday":
    print("You can sleep in")
```

**3.** False、True、False。and 全真才真；or 一真即真；not 真变假。

**4.**
```python
age = int(input("Age: "))

if age <= 12 or age >= 65:
    print("Half price: 20 yuan")
else:
    print("Full price: 40 yuan")
```

**5.**
```python
a = int(input("Score of subject 1: "))
b = int(input("Score of subject 2: "))
c = int(input("Score of subject 3: "))
avg = (a + b + c) / 3

if a >= 60 and b >= 60 and c >= 60 and avg >= 85:
    print("Scholarship candidate")
else:
    print("Keep it up")
```
四个条件一条 and 串到底，读作「每门都过线，且平均够高」。

## 九、自查清单

- [ ] and / or / not 我能口算（全真才真 / 一真即真 / 真假对调）
- [ ] 我知道「中间范围」要用 and 不是 or
- [ ] 成绩评定器跑通，还自己改过规则测了数据
- [ ] 我会主动给组合条件加小括号
- [ ] 练习 5 题完成，答案对过；🚀 挑战题做过（ThawPaw 选做，Tree 建议做）

全勾 → 进入 W03 周末项目「猜大小游戏 v1」。
